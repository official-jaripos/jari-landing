---
import { promoKemerdekaan } from '../../data/promo';

const isActive = promoKemerdekaan.isActive;
const { title, scarcityText, fakeProgressPercentage } = promoKemerdekaan;
---

{isActive && (
  <div id="promo-modal-backdrop" class="fixed inset-0 z-[100] bg-slate-900/60 backdrop-blur-sm hidden flex items-center justify-center p-4 opacity-0 transition-opacity duration-300">
    
    <div id="promo-modal-card" class="bg-white w-full max-w-lg rounded-3xl overflow-hidden shadow-2xl transform scale-95 transition-transform duration-300 relative">
      
      {/* Close Button */}
      <button id="close-promo-modal" class="absolute top-4 right-4 w-8 h-8 flex items-center justify-center bg-white/20 hover:bg-white/40 text-white rounded-full transition z-10">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
      </button>

      {/* Header Area (Red) */}
      <div class="bg-gradient-to-br from-[#8B0000] to-[#600000] p-8 text-center relative overflow-hidden">
        {/* Subtle pattern or glow could go here */}
        <div class="absolute -top-10 -right-10 w-32 h-32 bg-white/10 rounded-full blur-2xl"></div>
        
        <span class="inline-block px-3 py-1 bg-white/20 text-red-100 text-[10px] font-bold tracking-widest uppercase rounded-full mb-3 border border-white/20">
          Penawaran Terbatas
        </span>
        <h2 class="text-3xl md:text-4xl font-black font-display text-white tracking-tight leading-tight mb-2">
          {title}
        </h2>
        <p class="text-red-100 font-medium text-sm">
          Untuk Pengguna Baru!
        </p>
      </div>

      {/* Body Area */}
      <div class="p-8 text-center flex flex-col items-center">
        
        <p class="text-slate-700 font-bold text-lg mb-4">
          {scarcityText}
        </p>

        {/* Fake Progress Bar */}
        <div class="w-full max-w-xs flex flex-col gap-2 mb-8">
          <div class="flex justify-between text-xs font-bold text-slate-500 uppercase tracking-wider">
            <span>Kapasitas Terisi</span>
            <span class="text-[#8B0000]">{fakeProgressPercentage}%</span>
          </div>
          <div class="w-full h-2.5 bg-slate-100 rounded-full overflow-hidden shadow-inner">
            <div 
              class="h-full bg-[#8B0000] rounded-full transition-all duration-1000 ease-out"
              style={`width: ${fakeProgressPercentage}%`}
            ></div>
          </div>
        </div>

        <a href="https://app.jaripos.com/register" id="claim-promo-btn" class="w-full py-4 rounded-xl bg-[#8B0000] hover:bg-[#600000] active:scale-95 text-white font-black text-lg shadow-lg transition-all duration-300">
          Klaim Promo Sekarang
        </a>

      </div>
    </div>
  </div>

  <script>
    // Promo Modal Logic
    document.addEventListener('DOMContentLoaded', () => {
      const backdrop = document.getElementById('promo-modal-backdrop');
      const card = document.getElementById('promo-modal-card');
      const closeBtn = document.getElementById('close-promo-modal');
      const skipBtn = document.getElementById('skip-promo-btn');
      const claimBtn = document.getElementById('claim-promo-btn');
      
      if (!backdrop) return;

      // Show modal after 2 seconds of page load (every time)
      setTimeout(() => {
        backdrop.classList.remove('hidden');
        // small delay to allow display:block to apply before animating opacity
        requestAnimationFrame(() => {
          backdrop.classList.remove('opacity-0');
          card.classList.remove('scale-95');
          card.classList.add('scale-100');
        });
      }, 2000);

      function closeModal() {
        backdrop.classList.add('opacity-0');
        card.classList.remove('scale-100');
        card.classList.add('scale-95');
        
        // Wait for transition to finish
        setTimeout(() => {
          backdrop.classList.add('hidden');
        }, 300);
      }

      closeBtn?.addEventListener('click', closeModal);
      // We removed skipBtn but leaving claimBtn behavior intact (it will navigate before closing)
      claimBtn?.addEventListener('click', closeModal); 

      // Close on clicking backdrop
      backdrop.addEventListener('click', (e) => {
        if (e.target === backdrop) closeModal();
      });
    });
  </script>
)}
